import 'package:flutter/material.dart';

/// App color constants
class AppColors {
  AppColors._(); // Private constructor to prevent instantiation

  // Brand colors
  static const Color primary = Color(0xFF6C63FF);
  static const Color secondary = Color(0xFFFF6584);
  static const Color accent = Color(0xFF00D9FF);

  // Currency colors
  static const Color coin = Color(0xFFFFD700); // Gold color for coins
  static const Color gem = Color(0xFF00BFFF);  // Diamond blue for gems

  // Background colors
  static const Color background = Color(0xFF1A1A2E);
  static const Color backgroundLight = Color(0xFF16213E);
  static const Color backgroundDark = Color(0xFF0F0F1E);

  // UI colors
  static const Color success = Color(0xFF4CAF50);
  static const Color warning = Color(0xFFFF9800);
  static const Color error = Color(0xFFFF5252);
  static const Color info = Color(0xFF2196F3);

  // Text colors
  static const Color textPrimary = Color(0xFFFFFFFF);
  static const Color textSecondary = Color(0xFFB8B8B8);
  static const Color textDisabled = Color(0xFF757575);

  // Game button colors (8 colors for the memory game)
  static const Color buttonRed = Color(0xFFFF6B6B);
  static const Color buttonBlue = Color(0xFF4ECDC4);
  static const Color buttonGreen = Color(0xFF95E1D3);
  static const Color buttonYellow = Color(0xFFFFE66D);
  static const Color buttonPurple = Color(0xFFA8E6CF);
  static const Color buttonOrange = Color(0xFFFFAA5A);
  static const Color buttonPink = Color(0xFFFF8DC7);
  static const Color buttonTeal = Color(0xFF38B6FF);

  // Additional UI colors
  static const Color cardBackground = Color(0xFF2A2A3E);
  static const Color divider = Color(0xFF3A3A4E);
  static const Color overlay = Color(0x80000000);
}
